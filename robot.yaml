tasks:
  Run all tasks:
    shell: python -m robot --outputdir output --logtitle "Task log" --removekeywords passed --removekeywords for tasks.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
